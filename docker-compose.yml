version: '2'

volumes:
  homedir:

services:
  dev-env:
    image: mgnsk/dev-env:latest
    hostname: dev-env
    environment:
    - UID=${UID}
    - USER=${USER}
    - GID=${GID}
    - GROUP=${GROUP}
    working_dir: /code
    volumes:
    - homedir:/homedir
    - ./code:/code

