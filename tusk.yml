options:
  container_uid:
    default: 3000
  container_gid:
    default: 3000
  dot_url:
    default: https://github.com/mgnsk/dotfiles.git
  dot_commit:
    default: 23e52604af6acca2cea3d93f1b5be2003636d60d

tasks:
  build:
    run:
      - set-environment:
          UID: ${container_uid}
          GID: ${container_gid}
          DOT_URL: ${dot_url}
          DOT_COMMIT: ${dot_commit}
      - command: podman-compose build

  root:
    usage: Run a root shell.
    run:
      - command: mkdir -p ./mount
      - command: podman-compose -p dev-env run --rm --service-ports --user root dev-env /bin/bash

  bash:
    usage: Run bash shell as a non-root user.
    run:
      - command: mkdir -p ./mount
      # Files created by container users will be writable by the host.
      - command: podman unshare setfacl -Rdm u:0:rwX ./mount
      # Existing host files are writable by container users.
      - command: podman unshare setfacl -Rm g:${container_gid}:rwX ./mount
      # Files created by the host will be writable by container users.
      - command: podman unshare setfacl -Rdm g:${container_gid}:rwX ./mount
      - command: podman-compose -p dev-env run --rm --service-ports dev-env /bin/bash

  clean:
    run:
      - command: podman pod rm dev-env || true
      - command: podman rmi mgnsk/dev-env || true

  ci-build:
    run:
      - command: >
          docker
          build
          --pull
          --build-arg uid=${container_uid}
          --build-arg gid=${container_gid}
          --build-arg dot_url=${dot_url}
          --build-arg dot_commit=${dot_commit}
          -t mgnsk/dev-env:latest
          ./image

  ci-push:
    run:
      command: |
        echo "$DOCKER_TOKEN" | docker login -u mgnsk --password-stdin
        docker push mgnsk/dev-env:latest
