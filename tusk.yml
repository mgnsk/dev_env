---
options:
  container_uid:
    default: 3000
  container_gid:
    default: 3000
  dot_url:
    default: https://github.com/mgnsk/dotfiles.git
  dot_commit:
    default: 675ca2bcb9f6aef225aac3e01ec5808e0c9573b5

tasks:
  _make-user-mounts:
    run:
      - command: mkdir -p code
      - command: mkdir -p .fuse/code/{lowerdir,workdir,merged}
      - command: podman unshare fuse-overlayfs -o uidmapping=0:${container_uid}:1,gidmapping=0:${container_gid}:1,lowerdir=.fuse/code/lowerdir,upperdir=code,workdir=.fuse/code/workdir,noxattrs=1,fsync=1,threaded=1 .fuse/code/merged

  _make-root-mounts:
    run:
      - command: mkdir -p code
      - command: mkdir -p .fuse/code
      - command: ln -s $PWD/code $PWD/.fuse/code/merged

  _clean-mounts:
    run:
      - command: rm -rf .fuse
      - command: find ./code -type c -exec rm -f {} \;
      - command: find ./code -type f -name ".wh..wh..opq" -exec rm {} \;

  build:
    run:
      - command: >
          podman-compose
          build
          --pull
          --build-arg uid=${container_uid}
          --build-arg gid=${container_gid}
          --build-arg dot_url=${dot_url}
          --build-arg dot_commit=${dot_commit}
      - command: podman image prune -f

  bash:
    usage: Run bash shell as a non-root user.
    run:
      - task: _clean-mounts
      - task: _make-user-mounts
      - command: podman-compose -p dev-env run --rm --service-ports toolbox /bin/bash

  root:
    usage: Run a root shell.
    run:
      - task: _clean-mounts
      - task: _make-root-mounts
      - command: podman-compose -p dev-env run --rm --service-ports --user root toolbox /bin/bash

  kill:
    run:
      - command: podman pod kill dev-env || true

  drop-vols:
    run:
      - task: _clean-mounts
      - command: for v in $(podman volume ls | awk 'NR>1 {print $2}' | grep "^dev-env"); do podman volume rm $v; done

  drop-image:
    run:
      - command: podman pod rm dev-env || true
      - command: podman rmi mgnsk/toolbox || true

  destroy:
    run:
      - task: kill
      - task: drop-vols
      - task: drop-image

  ci-build:
    run:
      - command: >
          docker
          build
          --pull
          --build-arg uid=${container_uid}
          --build-arg gid=${container_gid}
          --build-arg dot_url=${dot_url}
          --build-arg dot_commit=${dot_commit}
          -t mgnsk/toolbox:latest
          ./image

  ci-push:
    run:
      command: |
        echo "$DOCKER_TOKEN" | docker login -u mgnsk --password-stdin
        docker push mgnsk/toolbox:latest
